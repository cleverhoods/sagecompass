# Local scope: app/state

This file is the **primary** instruction surface when editing `app/state/**`.

## Scope
- State owns the typed routing keys, SageState definitions, and state update validation.
- Use `component_types.state.contracts` in `../../.shared/components.yml` to find enforceable contracts and rule snippets.

## References
- Backend AGENTS: `../../AGENTS.md`
- Component map: `../../.shared/components.yml`
- System map: `../../.shared/sys.yml`
- Platform map: `../../.shared/platform.yml`
