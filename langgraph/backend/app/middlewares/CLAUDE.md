# Local scope: app/middlewares

This file is the **primary** instruction surface when editing `app/middlewares/**`.

## Scope
- Middleware enforces policy decisions, redaction, and deterministic tool interactions.
- Use `component_types.middlewares.contracts` in `../../.shared/components.yml` to find enforceable contracts and rule snippets.

## References
- Backend CLAUDE: `../../CLAUDE.md`
- Component map: `../../.shared/components.yml`
- System map: `../../.shared/sys.yml`
- Platform map: `../../.shared/platform.yml`