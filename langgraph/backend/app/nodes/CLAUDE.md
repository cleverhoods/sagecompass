# Local scope: app/nodes

This file is the **primary** instruction surface when editing `app/nodes/**`.

## Scope
- Node modules are orchestration-only factories that handle routing updates and logging.
- Use `component_types.nodes.contracts` in `../../.shared/components.yml` to find enforceable contracts and rule snippets.

## References
- Backend CLAUDE: `../../CLAUDE.md`
- Component map: `../../.shared/components.yml`
- System map: `../../.shared/sys.yml`
- Platform map: `../../.shared/platform.yml`