# `schemas/` — Shared Semantic Schemas

Shared semantic data definitions that are not bound to a single node/agent.
Prefer Pydantic for boundaries and persisted artifacts.

## Canonical rules
- Use `../.shared/components.yml` → `component_types.schemas` for schema versioning and provenance requirements.

## Key docs
- LangChain Structured Output (Pydantic schemas): https://docs.langchain.com/oss/python/langchain/structured-output
